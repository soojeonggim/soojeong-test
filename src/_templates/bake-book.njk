{% extends '_templates/base.njk' %}

{% set relative_path = "../" %}

{% block page_title %}{{ member.name }}{% endblock %}

{% block page_description %}Learn more about {{ member.name }}, a member of Queen.{% endblock %}

{% block content %}
  <article class="container">
    <div class="row">
      <div class="col-sm-4">
          <img src="../img/{{ slug }}.jpg" alt="" class="img-fluid mb-4">
      </div>
      <div class="col-sm-8">
          <h1>{{ title }}</h1>
          <p>{{ blurb | safe }}</p>
      </div>
    </div>
  </article>
{% endblock %}
